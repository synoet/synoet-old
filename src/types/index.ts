export type SEOMetaProps = {
  meta: {
    title: string
    description: string
  }
}

export type PositionProps = {
  company: string
  role: string
  range: string
}

export type ProjectProps = {
  title: string
  description: string
  link?: string
}

export type SkillProps = {
  name: string
  level?: string
}
